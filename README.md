# final-corzaar-service
final creating micro service.

run api gateway file, after to use api gateway end point. dont connect direct to micro service.

run command:
    
    cd ./api-gateway/
    npm run dev